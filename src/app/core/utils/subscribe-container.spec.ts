import { SubscribeContainer } from './subscribe-container';

describe('SubscribeContainer', () => {
  it('should create an instance', () => {
    expect(new SubscribeContainer()).toBeTruthy();
  });
});

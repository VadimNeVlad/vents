import { AvatarColorDirective } from './avatar-color.directive';

describe('AvatarColorDirective', () => {
  it('should create an instance', () => {
    const directive = new AvatarColorDirective();
    expect(directive).toBeTruthy();
  });
});
